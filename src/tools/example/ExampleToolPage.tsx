import React from 'react';

/** Example tool page to demonstrate code-splitting and routing. */
export const ExampleToolPage = (): React.ReactElement => {
	return (
		<div className="p-8">
			<h2 className="text-2xl font-bold">Example Tool</h2>
			<p>This is an example tool demonstrating code-split routing.</p>
		</div>
	);
};
